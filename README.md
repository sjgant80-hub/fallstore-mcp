# fallstore-mcp

MCP stdio server wrapping `@ai-native-solutions/fallstore-sdk`.

## Install
```bash
claude mcp add fallstore -- npx -y @ai-native-solutions/fallstore-mcp
```

MIT · AI-Native Solutions
